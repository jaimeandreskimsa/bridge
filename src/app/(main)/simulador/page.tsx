import { SimuladorClient } from "./simulador-client";

export const metadata = { title: "Simulador de Palo · Bridge Academy" };

export default function SimuladorPage() {
  return <SimuladorClient />;
}
